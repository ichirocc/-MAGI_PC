# #3 ホーム: 実行中の進捗（進捗行＋途中経過）

原本: `MagiScheduleViews.kt` `progressSummary`（3.393.0/3.396.0）、`LiveScheduleCard`。

- 進捗行（実行中のみ、処方箋カード内・スピナー付き）: 「必須違反 残りN件（開始M件）」または「気になる点 N件（開始M件・P%減）」
  （初期ソフト0なら「気になる点 –」）＋必須が残る間は「気になる点 全T件」＋「残り m:ss」（予算秒−経過）。区切りは「  ・  」。
  反復数・毎秒は出さない（作り手の指標）。
- 途中経過カード（実行中かつ liveSchedule あり）: 「途中経過を見る／隠す」トグル（既定は閉）。開くと
  「状態遷移  赤枠＝今回変化 (N)」＋ 職員×日の 11px 色タイル（シフト色、未割当は surfaceVariant、前回から変わったセルは赤枠2px）。横スクロール。
- 状態: ロード前・停止後は両方とも非表示。C# の `ElapsedMs/BudgetSec/InitHard/InitSoft/LiveSchedule` は VM が既に更新している＝VM 変更なし。
