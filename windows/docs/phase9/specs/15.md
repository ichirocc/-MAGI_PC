# #15 編集: 月次チェックリスト（職員/希望/必要人数/診断→つくる）

原本: `MagiSetupCards.kt` `MonthlyChecklistCard`／`ChecklistRow`（3.483.0 E-3 で入力診断をその場で開く形）。

- 「今月の作成条件」を 4 行で確認（read-only・スコアリング不変）。各行＝✓(第三色)/！(エラー色)＋ラベル＋値:
  1. 職員「N名」ok=N>0。
  2. 希望・休暇「M/N名 入力済み」ok=M>0（M=希望を 1 件以上持つ職員数）。タップで希望シフト登録へ（WinUI では同じパネル内の職員コンボへスクロール）。
  3. 必要人数「標準あり／標準が未設定・例外K件」ok=標準あり（need1 が空でないシフトが 1 つ以上）。例外は D6 に従い needDay の件数だけ。
  4. 入力診断「問題なし／見直しJ件 ▸/▾」ok=J==0。J>0 ならタップでその場に上位 6 件「・where：problem」＋「ほかN件（分析タブの設定見直しに全件）」。
- 「▶ 勤務表をつくる」ボタンは原本でも撤去済み（3.482.0、フッターに一本化）＝移植しない。
- VM: `MonthlyChecklist()`（`MonthlyChecklistView(StaffN, WishStaff, NeedStdOk, NeedExceptions, Issues)`）を追加。
