# #16 編集: 実働チェック（シフト別 担当人数・需要・欠勤余裕）

原本: `MagiSetupCards.kt` `StaffingRealityCard`（年度始めモード・D5 残スコープ）。Android では年間マスター（土台）ドアの先頭（見直し候補メモの直後）。

- 「この体制で回るか」＋説明 1 行「シフト別に「担当できる人数」で確認。余裕=1人欠けても回るか。」read-only・スコアリング不変。
- シフトごと（月間需要 D=Σ日次 lo が 0 のシフトは出さない。全て 0 ならカードごと出さない）:
  Q=担当できる人数（`AllowedShiftsFor` の集計）、D=月間需要人日、日最大=max 日次 lo、1人あたり=D/Q を小数1桁（四捨五入）。
  余裕 slack=Q−日最大。行＝印＋記号＋「担当Q人・月D人日（1人あたりX.X回）・」＋
  slack<0「日最大N人 → 担当不足」(⚠ エラー色) / slack==0「日最大N人 → 欠勤余裕なし」(！ 警告濃色) / それ以外「欠勤余裕S人」(✓ 第三色)。
- データは Problem 由来（`NeedCellLimits`=need1＋日別例外込みの実効値）＝チェッカーと同じ。
- VM: `StaffingReality()`（`StaffingRealityRow(Kigou, Q, D, MaxNeed)` の一覧）を追加。
