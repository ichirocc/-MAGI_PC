# #22 設定: 重み表（族・重み・HARD/SOFT）

原本: `MagiDashboardCards.kt` `WeightTableCard`（3.396.0 の文言。設定タブの最適化設定の隣＝実行条件の根拠として）。

- 見出し「直す優先順位」＋「上にあるものから先に直します。」。
- `MirrorKeys.Weights` を重い順に、1000 以上を「絶対に守る」（重みをエラー色）、未満を「できれば守る」に分けて
  「族名（BreakdownLabels）　×重み」（等幅）。英字符号（HARD/SOFT）は出さない。重みは整数なら整数表記。
- 定数なので起動時に 1 回だけ描画。VM 変更なし。#17 の締めの文「設定タブ →「直す優先順位」」の行き先がこれで実在になる。
