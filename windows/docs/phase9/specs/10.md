# #10 勤務表: シフト別不足サマリーの 1 行バナー（3.116.0）

原本: `MagiScheduleViews.kt` `ScheduleGrid` 冒頭（Web試作①）、日ヘッダの「▼N」（`ui.v6.dayRisks[d].shortage`）。

- バナー: `needViolations` のうち `vio-covU` を (シフト→日集合) に集計し、日数の多い順に「記号 N日」を「 ・ 」で連結。
  文言「人員不足（全T日中）: …」。赤容器。covU が無ければ非表示。「人員」バケツ OFF なら他の covU 表示と同様に隠す。
- 日ヘッダ「▼N」: その日の不足人数（`Ui.V6.DayRisks`）を日番号・曜日の下に赤太字で。「人員」バケツ ON のときだけ。今日の色は優先。
- VM 変更なし（`Ui.NeedViolations`・`Ui.V6` は既存）。
