# #1 ホーム: 被覆診断（人員不足の原因／人員過剰がなぜ減らないか）＋希望取消

原本: `MagiDashboardCards.kt` `CoverageDiagnosisCard(ui, onCancelWish)`（3.344.0 見出し場合分け／3.406.0 主因／3.492.0 希望取消）。

- 表示条件: `ui.coverageDiag` があり、不足か過剰のどちらかがある時だけ節を出す（無ければ節ごと隠す）。
- 不足: 見出し文 5 通り（全て充足不可／全て今は不能／今は不能が一部／充足不可0／それ以外）。枠は最大 6、
  各枠に「日 シフト 必要/現状（不足）」＋チップ（充足不可=赤／今は不能／充足可能）＋理由。7 枠目以降は「ほか N 枠（詳細はログ出力を参照）」。
  緩和案があれば「解けるようにするには（担当追加の案）」に最大 4 件＋注記。
- 過剰: 見出し「過剰 N 人 — …」。枠は最大 6、各枠に「日 シフト 必要/現状（過剰）」＋理由（主因: 日本語族名）。
  `pinnedStaff` の職員ごとにボタン「◯◯ の希望（記号）を取り消す」→ `removeWish(i, dayIndex)`。実行中は無効。
- 状態: ロード前＝診断なし＝非表示。実行中＝表示は残しボタンだけ無効。
- WinUI: `HomeView` に `CoveragePanel` を追加（`RenderCoverage`）。VM API は既存の `RemoveWish(int,int)` を使用＝VM 変更なし。
  族名は `AnalysisView.BreakdownLabels`（internal）へ委譲。チップ色は UD 固定の既存ブラシ（赤=MagiErrorBrush／他=MagiPrimaryBrush）。
