# #21 設定: 色ピッカー 36色（6×6・選択✓, 3.460.0）

原本: `ShiftColorEditor.kt` `ColorPickerDialog`＋`COLOR_PALETTE`（36 色、row0col0=#e08a1e アンカー）＋`pickFg`。

- 「変更」のフライアウトを差し替え: 上に「現在の色」スウォッチ、「色を選ぶ」、6×6 の 32px タイル。現在値と一致するタイルは主色の太枠＋「✓」
  （文字色は明度で自動、原本 `pickFg` と同式）。タイルの読み上げ名「色 #xxxxxx（・選択中）」。
- パレットは `MagiApp.ViewModels/ShiftColorPalette.cs`（原本と同順・同値、テストで 36 色・重複なし・アンカー位置を固定）。
- 「既定に戻す」は既存の行ボタンのまま。16進の直接入力は WinUI 既存の導線なので残す（原本には無い）。
- シフト色・違反基準色・族別色の全行が同じフライアウトを使う（`BuildColorRow` 経由）。VM 変更なし。
