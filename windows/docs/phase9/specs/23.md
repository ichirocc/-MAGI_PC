# #23 シェル: トップバーの状態バッジ＋下部コマンドバー

原本: `MagiApp.kt` `MagiTopBar`／`BottomCommandBar`（3.396.0/3.409.12 の文言、3.402.0 の「やめる」ゲート）。

- **トップバー**（NavigationView の上）: 主色の「MAGI」チップ＋現在の節名（ホーム/勤務表/編集/分析/設定）＋状態バッジ（読込後のみ）:
  実行中（必須N が残れば「実行中 必須N」、必須0なら soft の「init→best」）＝主色コンテナ／配布可（結果あり・必須0）＝第三色／
  必須違反 N＝エラー色／未計算＝無地。
- **下部コマンドバー**（NavigationView の下、常設）: 「元に戻す」「やり直し」は CanUndo/CanRedo かつ非実行中のときだけ。主ボタンは 1 本:
  実行中または提案探索中は「■ やめる」（`Stop` が両方を戻す）＝エラー色、結果なしは「▶ 勤務表をつくる」、結果ありは「▶ もう一度つくる」
  （どちらも `RunV6FullOptimize`）。読込前は無効。
- 幅 390dp 未満のアイコン化（3.497.0）は Windows では対象外（最小ウィンドウ幅が十分）。VM 変更なし。
