# #17 編集: 制約10族の「詳しい説明」ⓘ展開（3.409.14）

原本: `ConstraintHelp.kt`（family キー → 本文、Compose 非依存）＋ `ConstraintEditor.kt` `ConstraintHelpExpander`。

- 年間マスターの「登録済みのルール」の直上に、既定で閉じたリンク「ⓘ 詳しい説明（それぞれの条件の意味）」／開くと「ⓘ 詳しい説明を閉じる」。
- 開いたとき: 族ごとに「族の見出し（太字）＋本文（小さめ・薄め）」を10族分（`ConstraintFamilies`＋`SkillConstraintFamilies` の順）、
  末尾に共通の締め「条件どうしが衝突したときの優先順位は、設定タブ →「直す優先順位」の表のとおりです（上にあるものから先に直します）。」
- 本文は Android と同一文言（重みの数値は書かない＝HF77 で stale 化するため。必須かどうかだけ言う）。
- `ConstraintHelp`（`MagiApp.ViewModels`）に表を置き、テストで「10族のキーと過不足なく一致」を固定（Android の ConstraintHelpTest と同じ狙い）。
- WinUI に「直す優先順位」の重み表はまだ無い（#22 で移植予定）＝締めの文は原本どおり置き、#22 で導線が実在になる（blockers に気づきとして記録）。
