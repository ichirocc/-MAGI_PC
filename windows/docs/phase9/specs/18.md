# #18 分析: 要確認一覧（統合カード, 3.459.0/3.471.0）

原本: `MagiDashboardCards.kt` `AnalysisTriageCard`＋`AnalysisTriage.kt`（純関数）。queue の `ViolationHubCard`/`ConfirmListBody`/`AttentionBody`/`BreakdownBody` は
3.459.0 の名前で、3.471.0 で `AnalysisTriageCard` に置き換わっている（Android 側に該当シンボル無し）＝3.471.0 の形を移植する。

- 見出し「要確認」＋バッジ「計算済み／未計算」（`UiState.EngineRan`＝エンジンが走った事実。HasResult は手編集でも立つので使わない）。
- 上段「直さないと消えない項目」（エラー色の箱）: 必須族（groupViol/pref/covU/c3n）は常に。SOFT 族は診断が観測した結論だけで昇格
  （c1: `C1PlateauDiagnosis.HasEntries`、covO: `CoverageSurplus.BlockedFamily`）。理由文は診断の文言をそのまま（診断が無ければ空）。
  設定の破綻（`SettingIssues`）は種類ごとに 1 行へ集約（先頭2件の where＋「ほか」）、警告色の箱＋「設定へ」ボタン（編集タブへ）。
- 中段「計算後に残っている項目／エンジンが挑戦する項目（未計算）」: 残りの族を重み順に「・族名  N件/pt」＋断定しない注記。
- 下段「制約充足サマリー（正常 a / 残り b）」を既定で畳み、展開で ✔正常／⚠残っている の族名一覧。
- 実行中は「※実行中のため確定前の値です」。全部無ければ「確認事項はありません」。
- 必須違反の場所（Kotlin の ConfirmRow）は既存の「違反の場所」節（セルへジャンプ）が担う＝重ねて出さない。
- VM: `AnalysisTriage.Build(ui, labelOf)`（族の表示名は下流の `AnalysisView.BreakdownLabels` から受ける）、`UiState.EngineRan`
  （エンジン実行の各完了点で true、手編集・提案適用・CSV取込で false、復元は markResult）。テスト 8 件（原本 7 件＋EngineRan）。
